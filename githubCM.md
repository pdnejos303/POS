1. ดึงโค้ดล่าสุดมาก่อนเสมอ (Pull)
ก่อนเริ่มเขียนโค้ดใหม่ทุกครั้ง ต้องเช็กให้แน่ใจว่าโค้ดในเครื่องเราตรงกับบน GitHub เพื่อลดปัญหาโค้ดชนกันในภายหลัง

Bash
git checkout main    # สลับกลับมาที่กิ่งหลัก
git pull origin main # ดึงอัปเดตล่าสุดจาก GitHub มาที่เครื่องเรา
2. แตกกิ่งใหม่สำหรับทำฟีเจอร์ (Branching)
สมมติว่าเต้รับหน้าที่ทำระบบหลังบ้าน API หรือหน้า UI ใหม่ ให้สร้าง Branch ใหม่ และตั้งชื่อให้สอดคล้องกับงานไปเลย

Bash
git checkout -b feature/login-ui
# หรือตัวอย่างอื่นๆ เช่น
# git checkout -b feature/payment-api
(คำสั่ง -b หมายถึงการสร้าง Branch ใหม่และย้ายเข้าไปที่ Branch นั้นทันที)

3. เขียนโค้ดและบันทึกการเปลี่ยนแปลง (Add & Commit)
ระหว่างเขียนโค้ด แนวคิดแบบ "ทำให้เสร็จดีกว่าทำให้สมบูรณ์แบบ" (Finished over Perfect) เอามาปรับใช้ตรงนี้ได้ดีมากครับ ไม่ต้องรอให้ฟีเจอร์เสร็จสมบูรณ์ 100% ถึงค่อย Commit แนะนำให้ทยอยทำทีละส่วนให้ทำงานได้ตามเป้าหมายหลัก แล้ว Commit เก็บไว้เป็นระยะๆ จะจัดการได้ง่ายกว่า

Bash
git add .  # เตรียมไฟล์ทั้งหมดที่มีการแก้ไข
git commit -m "feat: สร้างหน้า Login เบื้องต้นและผูก API" # เขียนอธิบายสั้นๆ ว่าทำอะไรไป
4. ดันโค้ดขึ้น GitHub (Push)
เมื่อฟีเจอร์นั้นเสร็จระดับนึงแล้ว และรันเทสผ่านในเครื่องตัวเอง ให้ดัน Branch นี้ขึ้นไปเก็บบน GitHub

Bash
git push origin feature/login-ui
5. เปิด Pull Request (PR) บน GitHub
ขั้นตอนนี้เราจะไม่ได้ใช้ Command Line แล้ว แต่จะไปที่หน้าเว็บ GitHub ของโปรเจกต์ (เหมือนหน้ารูป POS ที่ส่งมาก่อนหน้านี้)

GitHub จะมีปุ่มสีเขียวโผล่ขึ้นมาว่า Compare & pull request ให้กดปุ่มนั้น

การเปิด PR คือการบอกเพื่อนว่า "เราทำฟีเจอร์นี้เสร็จแล้วนะ ช่วยตรวจหน่อย แล้วรวมเข้า main ให้ที"

เพื่อนสามารถเข้ามาดูโค้ด คอมเมนต์รีวิว และกดปุ่ม Merge pull request เพื่อรวมโค้ดเข้ากิ่งหลักได้

สิ่งสำคัญที่ควรรู้เพื่อให้ทำงานร่วมกันได้ราบรื่น
การคุยกันสำคัญมาก: ถ้าต้องทำฟีเจอร์ที่ไปแตะไฟล์ตั้งค่าหลัก (เช่น ไฟล์ Routing ใน Next.js หรือไฟล์ Config ต่างๆ) พยายามคุยกันก่อนว่าใครจะแก้ตรงไหน จะได้ไม่เกิดปัญหาโค้ดทับซ้อนกัน

รู้จักกับ Merge Conflict: ถ้าเกิดเหตุการณ์ที่ Git แจ้งเตือนว่ามี Conflict ไม่ต้องตกใจครับ มันคือสถานการณ์ที่ Git ไม่รู้ว่าจะเอาโค้ดของใครเป็นหลัก (เช่น เต้กับเพื่อนดันไปแก้โค้ดบรรทัดเดียวกันเป๊ะๆ) วิธีแก้คือเปิดไฟล์นั้นขึ้นมา เลือกว่าจะเก็บโค้ดของใครไว้ ลบส่วนที่ไม่ใช้ออก แล้วทำการ git add และ git commit อีกรอบ

อัปเดต Branch ตัวเองให้ทันสมัยอยู่เสมอ: ถ้าเพื่อนเพิ่งรวมโค้ดของเขาเข้า main ไปหมาดๆ เราควรดึงโค้ดล่าสุดของ main เข้ามาผสมใน Branch ที่เรากำลังทำอยู่ด้วย เพื่อเช็กว่าพอมันรวมกันแล้วพังไหม โดยใช้คำสั่ง git merge main (ในขณะที่เรายืนอยู่ใน Branch ของเรา)